import DocumentLayout from '@/components/layout/DocumentLayout';
import { termsData } from '@/utils/data/terms';

export default function TermsPage() {
  return <DocumentLayout data={termsData} />;
}
